# ============================================================
# PRODUCTION environment defaults (non-secret values only)
# Secret values must be set via CI/CD secrets or vault
# ============================================================

NODE_ENV=production
VITE_APP_ENV=production
TZ=UTC
PORT=80
SSL_PORT=443
